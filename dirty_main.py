import application.salary
# from application import * как тогда должна работать данная строка? Если при вызове функции, автоматически вылезает импорт сверху
# т.е. я импортирую всё со зведой, но не получаю ничего

application.salary.calculate_salary()
